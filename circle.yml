machine:
  services:
    - docker
  python:
    version: 3.4.2

dependencies:
  override:
    - pip install -U docker-compose
    - pip install -U tox

test:
  override:
    - mkdir -p $CIRCLE_TEST_REPORTS/pytest
    - tox
